import { TRouteProps } from "@/types"
import { routes } from "@/lib/dummy/routes"

/** Keep in sync with `routes.doctor` — single source for doctor portal nav. */
export const doctorRoutes: TRouteProps[] = routes.doctor
